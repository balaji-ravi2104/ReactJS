let url = "http://localhost:8080"
let flag = "true"
let score = 88
let db_config = {
    host: "localhost",
    user: "root",
    password: "root",
    database: "nodedb",
    table: "products"
}

export default url;
export {flag,score,db_config}